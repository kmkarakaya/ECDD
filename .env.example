# Example env file for Gemini proxy
# Copy to .env and set real values before running the server
GEMINI_API_KEY=
GEMINI_MODEL=gemini-nano-banana
# Optional: override API base URL
# GEMINI_API_URL=https://api.generativeai.googleapis.com/v1beta
